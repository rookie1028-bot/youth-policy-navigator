{
  "name": "youth-policy-navigator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 17542",
    "test": "vitest run",
    "build": "vite build"
  },
  "dependencies": {
    "@vitejs/plugin-legacy": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "vitest": "latest"
  }
}
